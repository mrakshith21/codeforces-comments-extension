import React, { Component } from 'react';

const CommentsClassifier = ({comments}) => {

    return (
        <div>
            This will classify all comments
        </div>
    )    
}

export default CommentsClassifier;